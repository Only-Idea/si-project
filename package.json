{
  "name": "si-3d-project",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5174 --strictPort",
    "build": "node scripts/validate-catalogue.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "check": "node scripts/validate-catalogue.mjs",
    "render:products": "node scripts/render-product-images.mjs",
    "test": "npm run build && playwright test",
    "price": "node scripts/fetch-price.mjs",
    "price:auth": "node scripts/fetch-price.mjs auth",
    "test:price": "node --test scripts/fetch-price.test.mjs"
  },
  "dependencies": {
    "gsap": "3.15.0",
    "lenis": "1.3.26",
    "three": "0.186.0"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "parse5": "^8.0.1",
    "three-gpu-pathtracer": "0.0.24",
    "three-mesh-bvh": "0.9.5",
    "three-render": "npm:three@0.181.1",
    "vite": "8.3.0"
  }
}
